export const styles = {
  container: (theme) => ({
    display: "flex",
    flexWrap: "wrap",
    justifyContent: { xs: "center", lg: "space-between" },
    gap: 0,
  }),
  img: (theme) => ({
    width: "80%",
  }),
  body: (theme) => ({
    display: "flex",
    flexDirection: "column",
    // alignItems: "center",
    gap: 5,
    width: "100%",
  }),
};
